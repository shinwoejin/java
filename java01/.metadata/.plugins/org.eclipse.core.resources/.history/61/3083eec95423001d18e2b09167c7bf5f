package chap06.note;

public class note03 {
	// 자바 버전 확인하기
	// javac -version //c는 컴파일을 뜻함
	
	// 상속
	// - 상속, 메소드 재정의, final 클래스, final 메소드(재정의 불가)
	// Super Class : 상속 해주는 클래스(Parent Class Base, Existing)
	// Suer Class 추가 중첩 재정의 => Sub Class
	// Sub Class : (Child Class, Derived Extended)
	
	// class : 객체를 정의해서 만들기 위하여
	// 추상클래스 : 클래스를 효과적으로 정의하기위해서 만드는것
	
	// 추상클래스의 용도
	// - 실체 클래스에 반드시 존재해야할 필드와 메소드의 선언
	//   (실체 클래스의 설계 규격 - 객체생성용이 아님)
	// - 실체 클래스에는 공통된 내용은 빠르게 물려받고, 
	//   다른 점만 선언하면 되므로 시간 절약
	
}
